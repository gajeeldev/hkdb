import { View, Text } from 'react-native';

export const CycleSlashScreen = ({ id }: { id: string | string[] }) => {
  return (
    <View>
      <Text style={{ color: 'white' }}>CycleSlashScreen</Text>
    </View>
  );
};
