import { View, Text } from 'react-native';

export const DashSlashScreen = ({ id }: { id: string | string[] }) => {
  return (
    <View>
      <Text style={{ color: 'white' }}>DashSlashScreen</Text>
    </View>
  );
};
