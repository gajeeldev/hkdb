import { View, Text } from 'react-native';

export const DreamgateScreen = ({ id }: { id: string | string[] }) => {
  return (
    <View>
      <Text style={{ color: 'white' }}>DreamgateScreen</Text>
    </View>
  );
};
