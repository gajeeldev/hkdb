import { View, Text } from 'react-native';

export const GreatSlashScreen = ({ id }: { id: string | string[] }) => {
  return (
    <View>
      <Text style={{ color: 'white' }}>GreatSlashScreen</Text>
    </View>
  );
};
