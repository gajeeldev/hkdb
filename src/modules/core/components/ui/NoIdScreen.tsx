import { View, Text } from 'react-native';

export const NoIdScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: 'white' }}>No ID</Text>
    </View>
  );
};
